development: &defaults
  adapter: mysql2
  encoding: utf8
  database: peatio_development
  hostname: mysql
  port: 3306
  username: root
  password:
  pool: 10
  reconnect: true

test:
  <<: *defaults
  database: peatio_test

production:
  <<: *defaults
  database: peatio_production
